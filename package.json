{
  "name": "hybrid-engine-co",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
